[bitbake]
src_uri = git://git.openembedded.org/bitbake
local_repo_dir = @os.getenv("LOCAL_REPO_DIR") + "/bitbake"
dest_dir = @os.getenv("DEST_DIR")+ "/bitbake"
branch = master
last_revision = f8590547a198a78334debdf14bf40acb50c22ecc

[openembedded-core]
src_uri = git://git.openembedded.org/opebembedded-core
local_repo_dir = @os.getenv("LOCAL_REPO_DIR") + "/opebembedded-core"
dest_dir = @os.getenv("DEST_DIR")
branch = master
last_revision = 9484fb04c0ed8b3e08c6773662dc7a0f01cb39a8

[meta-openembedded]
src_uri = git://github.com/tprrt/meta-openembedded.git
local_repo_dir = @os.getenv("LOCAL_REPO_DIR") + "/meta-openembedded"
dest_dir = @os.getenv("DEST_DIR")
branch = master-stabilized
last_revision = 438645aea93c52a0080b5304591eeae9c86c3566

[meta-yocto]
src_uri = git://git.yoctoproject.org/meta-yocto
local_repo_dir = @os.getenv("LOCAL_REPO_DIR") + "/meta-yocto"
dest_dir = @os.getenv("DEST_DIR")
branch = master
last_revision = 09cadd509b8cdbe02631646f7925f3fc76aa6299

[meta-selinux]
src_uri = git://github.com/tprrt/meta-selinux.git
local_repo_dir = @os.getenv("LOCAL_REPO_DIR") + "/meta-selinux"
dest_dir = @os.getenv("DEST_DIR") + "/meta-selinux"
branch = master-stabilized
last_revision = 3ea63fd2cd5797a8398a92958123271b9666d161

[meta-exiguous]
src_uri = git://github.com/tprrt/meta-exiguous.git
local_repo_dir = @os.getenv("LOCAL_REPO_DIR") + "/meta-exiguous"
dest_dir = @os.getenv("DEST_DIR")
branch = master
last_revision = 0adff2f389b5b085f0bfde004e2341d3272e99ab
